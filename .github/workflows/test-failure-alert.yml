name: Test Failure Alert

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Simulate CCISS source failure
        run: |
          echo "Simulating failure of the CCISS source download"
          exit 1